# Pinned to versions verified working in production (audit Fase F #36).
# Bumping any of these — especially undetected-chromedriver — should be
# tested manually because Chrome/CDP API changes can silently break scrapers.
beautifulsoup4==4.14.3
requests==2.32.5
selenium==4.36.0
feedparser==6.0.12
undetected-chromedriver==3.5.5
streamlit==1.50.0
pandas==2.3.3
resend==2.29.0
fastapi==0.128.8
uvicorn[standard]==0.39.0
psutil==7.0.0
# Task queue
celery==5.4.0
redis==5.2.1
flower==2.0.1
